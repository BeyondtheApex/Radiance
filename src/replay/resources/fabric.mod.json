{
  "schemaVersion": 1,
  "id": "radiance_replay_capture",
  "version": "${version}",
  "name": "Radiance Replay Capture",
  "description": "Optional development-only replay capture tooling for Radiance.",
  "authors": [
    "Radiance"
  ],
  "license": "GPL-3.0",
  "environment": "client",
  "entrypoints": {
    "client": [
      "com.radiance.replay.capture.ReplayCaptureClient"
    ]
  },
  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric": "*",
    "minecraft": "${minecraft_version}",
    "radiance": "*"
  }
}
